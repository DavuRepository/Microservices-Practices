server:
  port: 8191
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DavuRepository/Microservices-Practices.git
          search-paths: spring-config-store/*service

